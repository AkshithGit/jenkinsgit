pipeline {
    agent any 
    stages {
        stage("Build"){
            echo "hi this is a build stage"
        }

        stage("Test"){
            echo "hi this is a Test stage"
        }
        stage("deploy"){
            echo "hi this is a deploy stage"
        }
    }
}